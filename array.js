let a = [1, 3, 45, "This is it, we are here", [12, 16, [346, 70]], {name: "Franklin"}, [2, 8]];
// console.log(a)
// console.log(bb[0])
// console.log(bb[9])
// console.log(a[3])
// console.log(a.length): looks for the total index position in the array.
// console.log(cc.length)
// console.log(cc[o])
// console.log(a.slice(o, 3))
// console.log(a.splice(3, 1))
// console.log(a.splice(3, 1), "we are here")
// console.log(cc.push(6))
// console.log(cc.pop(6))
// console.log(cc.toString(cc));
// console.log(cc.fill(cc));
// console.log(cc.join(" "));
// console.log(cc.shift());
// console.log(cc.unshift("hello"))
// console.log(cc.shift());

//let bb = "This is it, we are here";
// let cc = [1, 2, 3, 4, 5, "This is me"];
// console.log(a.unshift("hello"))
// console.log(a[3].charAt(3));

//let name = "Peter"

//let convert = name.toLowerCase();
//let arr = convert.split("");
//let reverse = arr.reverse();
//let join = reverse.join("");
//console.log(join);
// retep
//console.log(a.flat())

// let aa = [1, 3, 15, [2, 5, 8, [4, 2]]]
// //console.log(aa.flat(2))
// //console.log(aa)
// let ab = aa.flat(2);
// console.log(ab.sort((a, b)=> {
//     return a - b;
// }))
// function dc() {}
// call back () => {}
// const dc = () => {}
//console.log(ab)
// let dc = ["this is me", "1 am", 3, 6, {name: "Peter"}];
// console.log(dc.map((el) => {
//     return el;
// }))

// forEach
/*console.log(dc.forEach((el) => {
    return el;
}))
*/
// console.log(ab.reduce((a, b) => {
//     return a + b;
// }, 15))

//console.log(ab.some(() => {
 //   return el;
//}))

// let cart = [];
// let itemA = {name: "bag", cost: 500};
// let itemB = {name: "shoe", cost: 3000};
// let itemC = {name: "neck chain", cost: 3500};
// let itemD = {name: "trouser", cost: 4000};

// cart.push(itemA);
// cart.push(itemB);
// cart.push(itemC);
// cart.push(itemD);

// console.log(cart);

// let d = cart.map((el) => {return el.cost;})
// let ff = d.reduce((a,b) => {return a+b
// 
// let naira = "\u20
// console.log("Total:" +naira+ 
// 
// console.log(`Your total purchase is ${ff}
// 
// let newVal = ff.toString();
// let convertArr = newVal.split("");
// let splice = convertArr.splice(2,0,",");
// let symbol = convertArr.unshift("₦");
// let join = convertArr.join("");
// console.log(join);

// console.log(`your total purchase is ${join}`);
/*
    lenght *
    slice *
    splice:* it can be used to replace a value in the array. it detects where it 
    wants to remove part from the array and returns it in a new array. 
    toString
    concat* joins arrays.
    push* returns the new length of the array by adding the value at the last.
    pop reduces the lenght of the array. it is the direct opposite of push. pop
    fill changes the values in the array into the value you pass in and takes
    the 
    join converts an array into string and removes the seperator.  
    shift returns the first values in the array.
    unshift adds value and takes the first values in the array.
    replace mutates the array by making the first values the last and vice versa.
    flat is used to convert a 3 or more dimensional array into 1 dimensional array.
    The map method helps us to iterate through the array and prints out the values in the array.
    FILTER takes contents from an array out.
    the reduce calculate the total numbers of arrays and returns the result as a number. 
    the some checks through the array to see whether there are matching value. it returns a boolean. 
    kinds of operators in javascript. They return boolean results.
    Datatypes that return falsy values.
    Null, Undefined, 0, []
    Datatypes that return truty values.
    Array []
    != 0
    !=
    1. Arithmetic Operators.
    +
    /
    *
    %
    --
    =>
    2. Comparison Operators.
        <
        >
        <=
        >=
        ?
        =
        ==
        ===
        !== 
        !
        &&
        -=
        +=
    */ 
   
        //  let myArr = [1, 6, 3, 9, 0]
        //  let myArr1 = [1]
        //   console.log(myArr.filter(el => {
        //     return el > 3;
        // }))

        // if (true) {
        //     console.log("statement");
        // } else {
        //     console.log("statement")
        // }
        // true ? console.log("statement") : console.log("statement")

        // let count = 0;
        // count--;
        // console.log(count);

    //     let myArr = [1, 6, 3, 9, 0, 20, 15];
    //     let samp = myArr.sort((a,b) => {
    //         return a -b;
    //     });
    //     let val = samp.length - 1;
    //     console.log(samp)
    //     console.log(val)
    //     console.log("Our final result is:", samp[val])
    //     console.log(Math.min(...myArr));

    //    console.log(
    //     myArr.reduce((a, b) => {
    //         return Math.max(a, b);
    //     }, +Infinity));
       
    //    console.log(
    //     myArr.reduce((a, b) => {
    //         return a > b ? a : b;
    //     }),
    //    );
        /*
        pick any two array methods and explain it like an engineer.

        */
        
    //   
    